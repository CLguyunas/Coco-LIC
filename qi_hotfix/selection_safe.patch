diff --git a/src/odom/odometry_manager.cpp b/src/odom/odometry_manager.cpp
index b0498b9..0000000 100644
--- a/src/odom/odometry_manager.cpp
+++ b/src/odom/odometry_manager.cpp
@@ -522,7 +522,11 @@ namespace cocolic
 
     double gain_sum = 0.0;
-    if (!qi_config_.selection_enable)
+    // Safety rule: when the candidate count does not exceed the configured
+    // budget, keep the complete observation set. Greedy selection is only
+    // used to reduce an actually over-budget set.
+    if (!qi_config_.selection_enable ||
+        static_cast<int>(qi_lidar_obs_.size()) <= budget)
     {
       for (auto &obs : qi_lidar_obs_)
       {
@@ -551,8 +555,6 @@ namespace cocolic
         }
         if (best_idx < 0)
           break;
-        if (k >= 6 && best_gain < qi_config_.selection_min_gain)
-          break;
 
         used[best_idx] = 1;
         qi_lidar_obs_[best_idx].selected = true;
@@ -560,11 +562,6 @@ namespace cocolic
         lambda += qi_lidar_obs_[best_idx].info;
         gain_sum += best_gain;
         qi_selected_lidar_obs_.push_back(qi_lidar_obs_[best_idx]);
-
-        const double coverage = QiClamp(
-            (QiLogDet(lambda) - base_logdet) / full_gain, 0.0, 1.0);
-        if (k + 1 >= 6 && coverage >= qi_config_.selection_info_ratio)
-          break;
       }
     }
@@ -638,7 +635,11 @@ namespace cocolic
 
     double gain_sum = 0.0;
-    if (!qi_config_.selection_enable)
+    // Apply the same conservative rule to visual observations. This avoids
+    // under-constraining the estimator when the full visual candidate set is
+    // already within the allowed budget.
+    if (!qi_config_.selection_enable ||
+        static_cast<int>(qi_visual_obs_.size()) <= budget)
     {
       for (auto &obs : qi_visual_obs_)
       {
@@ -667,8 +668,6 @@ namespace cocolic
         }
         if (best_idx < 0)
           break;
-        if (k >= 6 && best_gain < qi_config_.selection_min_gain)
-          break;
 
         used[best_idx] = 1;
         qi_visual_obs_[best_idx].selected = true;
@@ -676,11 +675,6 @@ namespace cocolic
         lambda += qi_visual_obs_[best_idx].info;
         gain_sum += best_gain;
         qi_selected_visual_obs_.push_back(qi_visual_obs_[best_idx]);
-
-        const double coverage = QiClamp(
-            (QiLogDet(lambda) - base_logdet) / full_gain, 0.0, 1.0);
-        if (k + 1 >= 6 && coverage >= qi_config_.selection_info_ratio)
-          break;
       }
     }
